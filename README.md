# alarm-clock
This Python program acts as a digital alarm clock. It imports modules like datetime for current time, time for delays, and winsound for alerts. It takes user input for the alarm time, runs an infinite loop checking if now == alarm_time, and triggers a buzzer sound when the times match.
